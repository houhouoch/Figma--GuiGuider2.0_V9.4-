# Versioned manifests / 版本化清单

`v9_all_20260729_context` is the validated structured Figma snapshot used for the controlled conversion.

`v9_all_20260729_context` 是本次受控转换使用的、经过完整性校验的 Figma 结构化快照。

The snapshot contains 31 design roots and 1,756 nodes. It must be treated as immutable test input. New captures should be written to a new version directory instead of replacing it.

该快照包含 31 个设计根节点和 1756 个节点。它应作为不可变测试输入；后续抓取必须创建新版本目录，不得原地覆盖。
